---
# role: samba_dc
# file: tasks/groups.yml

- name: SAMBA_DC | Manage domain groups before resolving memberships
  jomrr.samba.samba_group: &__samba_dc_group_options
    name: "{{ __samba_dc_group.name }}"
    path: "{{ __samba_dc_group.path | default(omit) }}"
    scope: "{{ __samba_dc_group.scope | default('global') }}"
    category: "{{ __samba_dc_group.category | default('security') }}"
    description: "{{ __samba_dc_group.description | default(omit) }}"
    gid_number: "{{ __samba_dc_group.gid_number | default(omit) }}"
    state: "{{ __samba_dc_group.state | default('present') }}"
  loop: "{{ samba_dc_groups }}"
  loop_control:
    loop_var: __samba_dc_group
    label: "{{ __samba_dc_group.name }}"

- name: SAMBA_DC | Manage memberships after all groups exist
  jomrr.samba.samba_group:
    <<: *__samba_dc_group_options
    members: "{{ __samba_dc_group.members }}"
    members_purge: "{{ __samba_dc_group.members_purge | default(samba_dc_group_members_purge) }}"
  loop: "{{ samba_dc_groups }}"
  loop_control:
    loop_var: __samba_dc_group
    label: "{{ __samba_dc_group.name }}"
  when:
    - (__samba_dc_group.state | default('present')) == 'present'
    - __samba_dc_group.members is defined
