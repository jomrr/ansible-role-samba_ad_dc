---
# role: samba_dc
# file: tasks/users.yml

- name: SAMBA_DC | Manage domain user accounts
  jomrr.samba.samba_user:
    username: "{{ __samba_dc_user.username }}"
    path: "{{ __samba_dc_user.path | default(omit) }}"
    given_name: "{{ __samba_dc_user.given_name | default(omit) }}"
    surname: "{{ __samba_dc_user.surname | default(omit) }}"
    display_name: "{{ __samba_dc_user.display_name | default(omit) }}"
    email: "{{ __samba_dc_user.email | default(omit) }}"
    description: "{{ __samba_dc_user.description | default(omit) }}"
    uid_number: "{{ __samba_dc_user.uid_number | default(omit) }}"
    gid_number: "{{ __samba_dc_user.gid_number | default(omit) }}"
    unix_home_directory: "{{ __samba_dc_user.unix_home_directory | default(omit) }}"
    login_shell: "{{ __samba_dc_user.login_shell | default(omit) }}"
    gecos: "{{ __samba_dc_user.gecos | default(omit) }}"
    enabled: "{{ __samba_dc_user.enabled | default(true) }}"
    password: "{{ __samba_dc_user.password | default(omit) }}"
    update_password: "{{ __samba_dc_user.update_password | default(samba_dc_user_update_password) }}"
    state: "{{ __samba_dc_user.state | default('present') }}"
  loop: "{{ samba_dc_users }}"
  loop_control:
    loop_var: __samba_dc_user
    label: "{{ __samba_dc_user.username }}"
  no_log: true
